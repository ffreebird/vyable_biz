# Vyable Biz - PRD (Product Requirements Document)

> **Version**: 1.0
> **Last Updated**: 2025-01-17
> **Status**: Draft
> **참조 문서**:
> - [Interview_Process_Log.md](./process/Interview_Process_Log.md) - 인터뷰 로직 명세
> - [RFP_Schema.md](./templates/RFP_Schema.md) - RFP 출력 스키마

---

## 1. 개요

### 1.1 프로젝트 정보

| 항목 | 내용 |
|------|------|
| **제품명** | Vyable Biz (바이어블 비즈) |
| **슬로건** | "AI Agent를 위한 완벽한 지시서 생성기" |
| **목표** | 비개발자의 아이디어를 AI 코딩 에이전트가 이해할 수 있는 구조화된 RFP로 변환 |

### 1.2 문제 정의

**문제**: 개발 지식이 없는 창작자가 AI 에이전트(Cursor 등)에게 코딩을 시키고 싶지만, 맥락(Context)을 제대로 전달하지 못해 쓸모없는 결과물만 얻는다.

**대상**: 1인 창작자, 비개발자 PM, 예비 창업가

**해결책**: 소크라테스 인터뷰(4 Phase, 20 Step)를 통해 사용자의 모호한 아이디어를 구조화된 맥락(RFP)으로 변환

### 1.3 성공 지표

| 지표 | 정의 | 목표 |
|------|------|------|
| **Zero-Shot Execution Rate** | 생성된 RFP로 AI가 추가 질문 없이 코드 생성하는 비율 | 100% 지향 |
| **인터뷰 완료율** | 시작한 사용자 중 RFP 생성까지 완료한 비율 | 70%+ |
| **RFP 완성도** | 확정 항목 비율 | 평균 80%+ |

---

## 2. 구현 범위

### 2.1 전체 범위 (Full Scope)

| 기능 | 설명 |
|------|------|
| **Phase 1: Seed (씨앗)** | Step 1~6: 서비스 정의, 타겟, 동작, 핵심 기능, 가치 제안, 플랫폼 |
| **Phase 2: Sprout (새싹)** | Step 7~10: 경쟁사 분석, 차별점, 최종 목표, 수익 모델 |
| **Phase 3: Tree (나무)** | Step 11~17: 유입 채널, 성공 지표, MVP 가설, 외부 연동, 초기 데이터, 운영 비용, 규제 |
| **Phase Final: 마무리** | Step 18~20: 비정형 맥락, Gap Detection, 최종 확정 |
| **RFP 생성** | 10개 섹션 구조화된 문서 생성 (Claude Opus 4.5) |
| **다운로드** | Markdown 파일 다운로드 |
| **프로젝트 관리** | 생성, 목록, 삭제, 저장/재개 |

### 2.2 Phase 전환 규칙

```
Phase 1 (Seed) 완료
    │
    ▼
"이 제품을 실제 시장에서 검증하길 원하시나요?"
    │
    ├─ "네" → Phase 2 (Sprout) 진행
    └─ "아니요/아직이요" → Phase Final로 이동

Phase 2 (Sprout) 완료
    │
    ▼
"이 서비스를 정식으로 출시하실 건가요?"
    │
    ├─ "네" → Phase 3 (Tree) 진행
    └─ "아니요/아직이요" → Phase Final로 이동

Phase 3 (Tree) 완료
    │
    ▼
자동으로 Phase Final 진행
```

> **중요**: Phase Final은 어느 단계에서 끝나더라도 항상 진행

---

## 3. 핵심 원칙

> **"질문의 목적은 정보 수집이 아니라 사고 촉발이다"**

### 3.1 파생 원칙

| # | 원칙 | 근거 |
|---|------|------|
| 1 | 사용자 결정만 묻는다 | 결정할 수 없는 걸 물으면 혼란 발생 |
| 2 | Yes/No가 아닌 선택을 준다 | Yes/No는 사고를 닫고, 선택지는 사고를 연다 |
| 3 | 먼저 제안하고 선택받는다 | 빈 종이는 막막함, 선택지는 출발점 |
| 4 | 놓친 관점을 드러낸다 | 모르던 걸 깨닫게 하는 게 사고 촉발 |
| 5 | AI가 채운 건 확정 안 함 | 사용자가 생각하지 않은 건 사용자 결정 아님 |
| 6 | 미정을 존중한다 | 억지로 결정하게 하면 진짜 사고 아님 |

### 3.2 응답 상태 분류

| 상태 | 의미 | RFP 반영 |
|------|------|----------|
| **[확정]** | 사용자가 명확히 답변 | 그대로 반영 |
| **[추론]** | AI가 맥락에서 유추 | 🔄 태그, 검증 필요 |
| **[미정]** | 사용자가 의도적으로 보류 | RFP 1.9에 기록 |
| **[누락]** | 질문하지 않아 정보 없음 | Gap filling에서 추가 |

---

## 4. 인터뷰 Step 명세

### Phase 1: Seed (제품 정의) - Step 1~6

#### Step 1: 서비스 정의

| 항목 | 내용 |
|------|------|
| **질문** | "어떤 서비스를 만들고 싶은가요?" |
| **타입** | text_input |
| **검증** | 3-Step Validation (Garbage → Active Inquiry → Pass) |
| **Pass 조건** | [대상], [행위], [가치] 중 2개 이상 포함 |
| **Max Inquiry** | 2회 |
| **RFP 매핑** | 1.1.1 |

#### Step 2: 타겟 정의

| 항목 | 내용 |
|------|------|
| **질문** | "이 서비스를 사용해야 하는 사람은 누구인가요?" |
| **타입** | single_select |
| **옵션** | A: 단일 역할 / B: 다중 역할 / C: 모르겠다 |
| **후속** | B 선택 시 → Step 2-1 (상호작용 규명) |
| **RFP 매핑** | 1.2.1 |

#### Step 2-1: 상호작용 규명 (조건부)

| 항목 | 내용 |
|------|------|
| **조건** | Step 2에서 B(다중 역할) 선택 시 |
| **질문** | "[역할A]와 [역할B]는 어떤 관계인가요?" |
| **타입** | single_select |
| **옵션** | 요청-수행 / 거래-교환 / 제공-소비 / 협업 / 직접 설명 |
| **RFP 매핑** | 1.2.1 |

#### Step 3: 동작(Action) 도출

| 항목 | 내용 |
|------|------|
| **질문** | "이 서비스에서는 어떤 동작들이 진행되어야 하나요?" |
| **타입** | multi_select_with_status |
| **AI 액션** | 서비스 정의 기반 핵심 동작 5~7개 제안 |
| **상태 옵션** | 필수 / 나중에 / 불필요 / 미정 |
| **Fallback** | 제안 불가 시 카테고리 기반 선택 |
| **RFP 매핑** | 1.3.1, 1.3.2, 1.3.3, 1.9 |

#### Step 4: 핵심 동작(Core) 선정

| 항목 | 내용 |
|------|------|
| **질문** | "이 동작들 중 제일 핵심이 되는 하나는 무엇인가요?" |
| **타입** | single_select |
| **옵션** | Step 3에서 '필수'로 선택된 항목들 |
| **검증** | 필수성 검증 → 독자성 검증 |
| **RFP 매핑** | 1.3.1 (Core 표시) |

#### Step 5: 가치 제안

| 항목 | 내용 |
|------|------|
| **질문** | "고객들이 이 서비스를 이용해야 하는 이유는 무엇인가요?" |
| **타입** | text_input |
| **검증** | 가격 기반(약함) / 기능 기반(중립) / 결과 기반(강함) |
| **Max Inquiry** | 3회 |
| **RFP 매핑** | 1.1.2 |

#### Step 6: 실행 환경

| 항목 | 내용 |
|------|------|
| **질문** | "이 서비스의 실행 환경은 어딘가요?" |
| **타입** | single_select |
| **옵션** | 웹 / 모바일 앱 / 둘 다 / 미정 |
| **후속** | "둘 다" 선택 시 우선순위 질문 |
| **맥락 검증** | 타겟-플랫폼 적합성 체크 |
| **RFP 매핑** | 1.5.1 |

---

### Phase 2: Sprout (시장 검증) - Step 7~10

#### Step 7: 경쟁 서비스 검색

| 항목 | 내용 |
|------|------|
| **액션** | AI 웹 서치로 경쟁 서비스 3개 제시 |
| **확인** | "이 서비스들이 경쟁 서비스가 맞나요?" |
| **옵션** | 맞아요 / 일부만 / 전혀 아님 / 경쟁사 없음 / 모르겠음 |
| **Max 재검색** | 2회 |
| **RFP 매핑** | 1.1.3 |

#### Step 8: 차별점 도출

| 항목 | 내용 |
|------|------|
| **조건** | Step 7에서 경쟁사 확인된 경우 |
| **질문** | "경쟁 서비스들 대신 이 서비스를 사용해야 하는 이유는?" |
| **타입** | single_select + text_input |
| **AI 액션** | 경쟁사 약점 분석 후 차별점 3가지 제안 |
| **RFP 매핑** | 1.1.3 |

#### Step 9: 최종 목표

| 항목 | 내용 |
|------|------|
| **질문** | "사용자는 이 서비스를 통해 최종적으로 어떤 상태가 되길 바라나요?" |
| **타입** | single_select + text_input |
| **검증** | 행위/제품 특성(불완전) → 사용자 상태(Pass) |
| **RFP 매핑** | 1.2.2 |

#### Step 10: 수익 모델

| 항목 | 내용 |
|------|------|
| **질문** | "이 서비스의 수익 모델은 어떻게 되나요?" |
| **타입** | single_select |
| **옵션** | 구독 / 건당 결제 / 수수료 / 광고 / 무료 / 미정 |
| **RFP 매핑** | 1.4.1 |

---

### Phase 3: Tree (사업 운영) - Step 11~17

#### Step 11: 유입 채널

| 항목 | 내용 |
|------|------|
| **질문** | "고객이 이 서비스를 처음 알게 되는 곳은 어디일까요?" |
| **옵션** | 검색 / SNS / 지인 추천 / 광고 / 미정 |
| **후속** | 선택에 따른 필수 기능 연결 |
| **RFP 매핑** | 1.1.4 |

#### Step 12: 성공 지표

| 항목 | 내용 |
|------|------|
| **질문** | "이 서비스가 잘 되고 있다는 걸 어떻게 알 수 있을까요?" |
| **AI 액션** | 서비스 유형별 지표 제안 |
| **후속** | 목표 수치 질문 |
| **RFP 매핑** | 1.6.1 |

#### Step 13: MVP 검증 가설

| 항목 | 내용 |
|------|------|
| **질문** | "첫 버전으로 확인하고 싶은 게 뭔가요?" |
| **옵션** | 문제 검증 / 솔루션 검증 / 지불 의향 / 리텐션 / 미정 |
| **후속** | 검증 기준 질문 |
| **RFP 매핑** | 1.6.2 |

#### Step 14: 외부 연동

| 항목 | 내용 |
|------|------|
| **조건** | Step 3에서 외부 서비스 필요 기능 선택 시 |
| **질문** | "외부 연동이 필요한 부분이 있나요?" |
| **AI 액션** | 선택된 기능에서 연동 유형 추론 (결제, 로그인, 알림 등) |
| **RFP 매핑** | 1.5.2 |

#### Step 15: 초기 데이터

| 항목 | 내용 |
|------|------|
| **질문** | "서비스 시작할 때 미리 준비해야 하는 데이터가 있나요?" |
| **옵션** | 있음(후속 질문) / 없음 / 모르겠음 |
| **RFP 매핑** | 1.5.4 |

#### Step 16: 운영 비용

| 항목 | 내용 |
|------|------|
| **질문** | "이 서비스를 운영하면서 발생할 비용은 어떤 게 있을까요?" |
| **AI 액션** | 플랫폼/기능에서 비용 요소 추출 |
| **후속** | 비용 충당 방법 질문 |
| **RFP 매핑** | 1.4.2 |

#### Step 17: 규제 제약

| 항목 | 내용 |
|------|------|
| **질문** | "이 서비스와 관련된 법적 규제나 인허가가 있을까요?" |
| **AI 액션** | 서비스 도메인에서 잠재 규제 추론 |
| **RFP 매핑** | 1.5.3 |

---

### Phase Final: 마무리 - Step 18~20

#### Step 18: 비정형 맥락 수집

| 항목 | 내용 |
|------|------|
| **질문** | "(선택) 그 외에 이 서비스를 이해하는데 필요한 내용이 있나요?" |
| **타입** | text_input (optional) |
| **처리** | AI가 분류하여 RFP 해당 섹션에 매핑 |

#### Step 19: Gap Detection & Fill

| 항목 | 내용 |
|------|------|
| **AI 액션** | RFP 초안 생성 후 논리적 공백 감지 |
| **우선순위** | P0(필수) → P1(권장) → P2(선택) |
| **질문 수** | 최대 5개 |
| **형식** | "[옵션 A vs B]" 형태로 제시 |

**Gap 우선순위**:

| 우선순위 | 기준 | 처리 |
|----------|------|------|
| P0 | MVP 구현 필수 | 반드시 질문 |
| P1 | 있으면 좋음 | 최대 3개 |
| P2 | 나중에 가능 | 스킵, RFP 1.9 기록 |

#### Step 20: 최종 확정

| 항목 | 내용 |
|------|------|
| **액션 1** | RFP 요약 제시 (섹션별 + 상태 표시) |
| **액션 2** | 완성도 체크 |
| **액션 3** | 최종 확인 (확정 / 수정 / 나중에) |
| **액션 4** | RFP 저장 |

**완성도 기준**:

| 완성도 | 확정 가능 | 안내 |
|--------|----------|------|
| 🟢 80%+ | ✅ | "RFP가 완성되었습니다" |
| 🟡 50~79% | ✅ | "추가 정보 필요 항목 있음" |
| 🟠 30~49% | ✅ | "미정 항목 많음, 리스크 경고" |
| 🔴 30% 미만 | ❌ | 필수 3개(서비스 정의, 타겟, 핵심 기능) 추가 질문 |

---

## 5. RFP 출력 스키마

> 상세: [RFP_Schema.md](./templates/RFP_Schema.md)

```
1.1 문제 정의
    1.1.1 해결하려는 문제
    1.1.2 기존 해결책과 한계
    1.1.3 경쟁/대체재 분석
    1.1.4 유입 채널
1.2 사용자 정의
    1.2.1 사용자 유형
    1.2.2 사용자별 핵심 목표
1.3 기능 범위
    1.3.1 핵심 기능 (Must)
    1.3.2 부가 기능 (Nice to have)
    1.3.3 제외 기능 (Out of scope)
1.4 수익/비용 구조
    1.4.1 수익 모델
    1.4.2 운영 비용
1.5 기술 제약
    1.5.1 플랫폼 결정
    1.5.2 외부 의존성
    1.5.3 규제 제약
    1.5.4 데이터 제약
1.6 성공 정의
    1.6.1 핵심 지표
    1.6.2 MVP 검증 가설
1.7 용어 정의
1.8 핵심 시나리오 개요
1.9 결정 보류 항목
1.10 브랜딩
```

---

## 6. 데이터 모델

### 6.1 ERD

```
┌──────────────┐       ┌──────────────────┐       ┌──────────────┐
│   Project    │       │  InterviewStep   │       │     RFP      │
├──────────────┤       ├──────────────────┤       ├──────────────┤
│ id (PK)      │──┐    │ id (PK)          │       │ id (PK)      │
│ name         │  │    │ projectId (FK)   │──┐    │ projectId(FK)│
│ status       │  └───▶│ stepNumber       │  │    │ content      │
│ currentPhase │       │ phase            │  │    │ completeness │
│ currentStep  │       │ question         │  │    │ version      │
│ createdAt    │       │ answer           │  │    │ createdAt    │
│ updatedAt    │       │ answerState      │  │    └──────────────┘
└──────────────┘       │ options          │  │
                       │ rfpMapping       │  │
                       │ createdAt        │  │
                       └──────────────────┘  │
                                             │
                       ┌──────────────────┐  │
                       │   RFPSection     │  │
                       ├──────────────────┤  │
                       │ id (PK)          │  │
                       │ projectId (FK)   │──┘
                       │ sectionKey       │
                       │ content          │
                       │ state            │
                       └──────────────────┘
```

### 6.2 스키마

```typescript
// packages/shared/src/schema.ts

import { z } from 'zod';

// Phase 타입
export const PhaseSchema = z.enum(['seed', 'sprout', 'tree', 'final']);

// Project
export const ProjectSchema = z.object({
  id: z.string().uuid(),
  name: z.string().min(1).max(100),
  status: z.enum(['draft', 'interviewing', 'completed']),
  currentPhase: PhaseSchema,
  currentStep: z.number().int().min(1).max(20),
  createdAt: z.date(),
  updatedAt: z.date(),
});

// Interview Step
export const AnswerStateSchema = z.enum(['confirmed', 'inferred', 'pending', 'missing']);

export const InterviewStepSchema = z.object({
  id: z.string().uuid(),
  projectId: z.string().uuid(),
  stepNumber: z.number().int().min(1).max(20),
  phase: PhaseSchema,
  question: z.string(),
  answer: z.union([z.string(), z.array(z.string())]).nullable(),
  answerState: AnswerStateSchema,
  options: z.array(z.object({
    id: z.string(),
    label: z.string(),
    hint: z.string().optional(),
    status: z.enum(['required', 'later', 'excluded', 'pending']).optional(),
  })).nullable(),
  rfpMapping: z.string(),
  createdAt: z.date(),
});

// RFP Section
export const RFPSectionSchema = z.object({
  id: z.string().uuid(),
  projectId: z.string().uuid(),
  sectionKey: z.string(),  // "1.1.1", "1.3.1" 등
  content: z.string(),
  state: AnswerStateSchema,
});

// RFP (전체)
export const RFPSchema = z.object({
  id: z.string().uuid(),
  projectId: z.string().uuid(),
  content: z.string(),  // 전체 Markdown
  completeness: z.number().int().min(0).max(100),
  version: z.number().int().min(1),
  createdAt: z.date(),
});
```

---

## 7. API 명세

### 7.1 엔드포인트

| Method | Endpoint | 설명 | AI 모델 |
|--------|----------|------|---------|
| **Projects** | | | |
| GET | `/api/projects` | 프로젝트 목록 | - |
| POST | `/api/projects` | 프로젝트 생성 | - |
| GET | `/api/projects/:id` | 프로젝트 상세 | - |
| DELETE | `/api/projects/:id` | 프로젝트 삭제 | - |
| **Interview** | | | |
| GET | `/api/projects/:id/interview` | 현재 인터뷰 상태 | - |
| POST | `/api/projects/:id/interview/next` | 다음 질문 생성 | Gemini Flash |
| POST | `/api/projects/:id/interview/answer` | 답변 제출 | Gemini Flash |
| POST | `/api/projects/:id/interview/validate` | 답변 검증 | Gemini Flash |
| POST | `/api/projects/:id/interview/phase-check` | Phase 전환 체크 | Gemini Flash |
| **RFP** | | | |
| GET | `/api/projects/:id/rfp` | RFP 조회 | - |
| POST | `/api/projects/:id/rfp/generate` | RFP 생성 | **Claude Opus** |
| POST | `/api/projects/:id/rfp/gap-detect` | Gap Detection | Gemini Flash |
| GET | `/api/projects/:id/rfp/download` | 다운로드 | - |

### 7.2 주요 API 상세

#### POST `/api/projects/:id/interview/next`

```typescript
// Request
{
  "currentStep": 6,
  "currentPhase": "seed"
}

// Response
{
  "step": 7,
  "phase": "sprout",
  "phaseTransition": {
    "question": "이 제품을 실제 시장에서 검증하길 원하시나요?",
    "options": ["네", "아니요", "아직이요"]
  }
}
// 또는 일반 질문
{
  "step": 2,
  "phase": "seed",
  "question": "이 서비스를 사용해야 하는 사람은 누구인가요?",
  "type": "single_select",
  "options": [...],
  "rfpMapping": "1.2.1"
}
```

#### POST `/api/projects/:id/interview/validate`

```typescript
// Request
{
  "stepNumber": 1,
  "answer": "쇼핑몰"
}

// Response
{
  "valid": false,
  "reason": "서비스 정의가 너무 추상적입니다",
  "inquiry": {
    "question": "[누가] 사용하고, [무엇을] 하는 서비스인가요?",
    "attempt": 1,
    "maxAttempts": 2
  }
}
```

#### POST `/api/projects/:id/rfp/generate`

```typescript
// Request
{
  "finalConfirm": true
}

// Response
{
  "id": "uuid",
  "content": "# 프로젝트명 - RFP\n\n## 1.1 문제 정의...",
  "completeness": 85,
  "stats": {
    "confirmed": 15,
    "inferred": 3,
    "pending": 2
  },
  "sections": [
    { "key": "1.1.1", "state": "confirmed" },
    { "key": "1.1.2", "state": "inferred" },
    ...
  ]
}
```

---

## 8. UI 화면

### 8.1 화면 목록

| 화면 | 경로 | 설명 |
|------|------|------|
| 랜딩 | `/` | 서비스 소개, CTA |
| 프로젝트 목록 | `/projects` | 내 프로젝트 목록 |
| 인터뷰 | `/projects/:id/interview` | 채팅 형식 인터뷰 |
| RFP 미리보기 | `/projects/:id/preview` | 완성된 RFP 확인 |

### 8.2 인터뷰 화면 상세

```
┌─────────────────────────────────────────────────────────────┐
│  [프로젝트명]                                                │
│  ────────────────────────────────────────────────────────── │
│  Phase: Seed (씨앗)              Step 3/6                   │
│  ████████████░░░░░░░░                                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 🤖 AI                                                │    │
│  │                                                      │    │
│  │ 이 서비스에서는 어떤 동작들이 진행되어야 하나요?     │    │
│  │                                                      │    │
│  │ 서비스 정의를 바탕으로 핵심 동작을 제안해 드릴게요.  │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 각 기능의 상태를 선택해주세요:                       │    │
│  │                                                      │    │
│  │ ☑️ 포트폴리오 등록      [ 필수 ▼ ]                  │    │
│  │ ☑️ 의뢰 등록           [ 필수 ▼ ]                  │    │
│  │ ☑️ 메시지/채팅         [ 나중에 ▼ ]                │    │
│  │ ☑️ 결제               [ 필수 ▼ ]                  │    │
│  │ ☑️ 리뷰/평점          [ 나중에 ▼ ]                │    │
│  │                                                      │    │
│  │ [ + 기능 추가 ]                                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ [ 이전 ]                              [ 다음 → ]   │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  [ 저장하고 나중에 계속 ]                                   │
└─────────────────────────────────────────────────────────────┘
```

### 8.3 Phase 전환 화면

```
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 🎉 Phase 1: Seed 완료!                              │    │
│  │                                                      │    │
│  │ 제품의 기본 정의가 완성되었습니다.                   │    │
│  │                                                      │    │
│  │ ──────────────────────────────────────────────────  │    │
│  │                                                      │    │
│  │ 이 제품을 실제 시장에서 검증하길 원하시나요?        │    │
│  │                                                      │    │
│  │ ┌─────────────────────────────────────────────┐     │    │
│  │ │ [ 네, 시장 검증 진행 ]                      │     │    │
│  │ │   → Phase 2: Sprout (경쟁 분석, 차별점)     │     │    │
│  │ └─────────────────────────────────────────────┘     │    │
│  │                                                      │    │
│  │ ┌─────────────────────────────────────────────┐     │    │
│  │ │ [ 아니요, 여기서 마무리 ]                   │     │    │
│  │ │   → Phase Final로 이동 (RFP 정리)           │     │    │
│  │ └─────────────────────────────────────────────┘     │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. AI 엔진 역할 분담

| 역할 | 모델 | 호출 빈도 |
|------|------|----------|
| 질문 생성 | Gemini 3.0 Flash | ~20회/세션 |
| 답변 검증 | Gemini 3.0 Flash | ~20회/세션 |
| Gap Detection | Gemini 3.0 Flash | 1~5회/세션 |
| **RFP 생성** | **Claude Opus 4.5** | 1회/세션 |

---

## 10. 구현 로드맵

### Phase 0: 프로젝트 세팅

- [ ] Monorepo 구조 (Bun Workspaces)
- [ ] 패키지 설치 (Hono, Drizzle, React, Vite, Tailwind)
- [ ] DB 스키마 및 마이그레이션
- [ ] 환경변수 설정

### Phase 1: 백엔드 기반

- [ ] Project CRUD API
- [ ] Interview State 관리
- [ ] Gemini 연동 (질문 생성, 검증)
- [ ] Claude 연동 (RFP 생성)

### Phase 2: 인터뷰 엔진

- [ ] Step 1~6 (Seed) 로직
- [ ] Step 7~10 (Sprout) 로직
- [ ] Step 11~17 (Tree) 로직
- [ ] Step 18~20 (Final) 로직
- [ ] Phase 전환 로직

### Phase 3: 프론트엔드

- [ ] 랜딩 페이지
- [ ] 프로젝트 관리
- [ ] 인터뷰 UI (채팅, 선택지, 상태)
- [ ] Phase 전환 UI
- [ ] RFP 미리보기 & 다운로드

### Phase 4: 통합 및 테스트

- [ ] E2E 테스트
- [ ] RFP 품질 검증
- [ ] 프롬프트 튜닝
